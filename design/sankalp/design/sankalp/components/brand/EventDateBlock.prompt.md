**EventDateBlock** — the editorial date lockup from event rows and cards: a large terracotta serif day over an uppercase sans month and faint year.

```jsx
<EventDateBlock day="14" month="Jun" year="2026" />
```

`size` is `sm | md | lg` (lg is the list-row scale).
