# System — persona backlog

Non-human persona owning cross-cutting/infra concerns: the auth/RLS engine, consent/audit/retention, notifications infra, **CI/CD + repo/toolchain**, monitoring, and the governance hooks (§12.12).

| functionality | priority | owner | consumers | scope | status |
|---|---|---|---|---|---|
| [repository-bootstrap](repository-bootstrap.md) | POC-core (foundational) | System | all 6 personas + every later System item | infra / pre-data (no RLS) | Built — review-clean, ready for `/test` ([plan](repository-bootstrap.plan.md)) |
| [core-schema-and-rls](core-schema-and-rls.md) | POC-core (foundational) | System | all 6 personas + auth-hook/notifications/chat/CSV-import System items | infra / data-layer (RLS-on, no auth-hook wiring) | Built — ADR-0021 attendance-write RPC landed; full suite green (73/73) ([plan](core-schema-and-rls.plan.md)); ADR-0031 `sessions.day_of_week`/`start_time`/`end_time` migrated + pgTAP-covered. **Addendum in flight:** `class_meetings`/`class_updates` (ADR-0034/0035, reconciled by ADR-0036, for `coordinator/compliance-dashboard`) — `/design` ✓ (signed off 2026-07-24) → `/plan` ✓ (Tasks 12–14) → `/migration` ✓ → `/build` ✓ → `/test` ✓ (2026-08-17, pgTAP 25 files/**324**). Shipped via PR #50; `class_meetings` is populated by ADR-0038's `classes_generate_class_meetings` trigger on `classes` insert (`20260729093000`), with that migration's backfill covering environments that already hold classes. **Note:** the Tasks 12–14 block in [`core-schema-and-rls.plan.md`](core-schema-and-rls.plan.md) is a pre-reconciliation historical record, not the schema contract — it carries a banner; read this spec's addendum section instead |
| [auth-hook-and-identity](auth-hook-and-identity.md) | POC-core (foundational) | System | all 6 personas | identity / JWT claims (plugs into `user_roles`) | Built + `/test` ✓ — ADR-0023 hook/switch/auto-activation landed; full suite green (100/100); live AC#7 sign-in pass verified 2026-07-11; ready for `/promote` (PR #11) ([plan](auth-hook-and-identity.plan.md)) |
| [client-auth-session-and-nav](client-auth-session-and-nav.md) | POC-core (foundational — client gate) | System | all 6 personas (gates #18–#24) | client shell — magic-link sign-in + session provider + role-derived nav + conditional active-role switcher | **Merged** (PR #30, `1e15253`, 2026-07-18) — issue #17 closed. `user_roles_self_select` migration + `lib/auth/` TDD landed; 233/233 vitest, 169/169 pgTAP, typecheck clean, `expo export --platform web` clean; RLS adversarial 24/24; live playwright pass over every AC incl. AC#9 live-switch and AC#10 web-reload; design review vs. Sankalp `USAGE.md` DoD found + fixed 3 real gaps ([issue #29](https://github.com/mehtamaulik-creator/CMDFW-BalaVihar---Pilot-App/issues/29), fixed + closed). PR review (arunasharad-coder) found a real web sign-in double-code-exchange bug (fixed) and flagged the minors'-PII audit exemption (resolved via **ADR-0027**, ADR-0019 addendum); approved. One disclosed, non-blocking gap: AC#10 native app-kill/restart unverified (no simulator tooling here) ([plan](client-auth-session-and-nav.plan.md)). **Gate lifted — #20–#24 unblocked** **Reopened** for [issue #46](https://github.com/cmdfw-bv/CMDFW-BalaVihar-App/issues/46) (`/no-role` dead end — no sign-out/auto-recovery) — Refined ✓, Architect ✓ (no ADR — contained client-only fix; 60s retry interval + `Button` reuse resolved inline), Design ✓ (`useAutoRefreshOnRegain` hook + `components/core/Button` reuse specced, no RLS/migration impact), human sign-off ✓ (2026-07-25), Plan ✓ (amendment plan appended 2026-07-25), Built ✓ (G1–G4 landed + manually verified), `/test` ✓ (2026-07-25, G5) — typecheck clean, design parity checked live at 360/768/1024/1440, UAT-9b/-9c added and passed live via playwright-cli + real magic-link. PR #54 review follow-up (`ae1e034`) added unit coverage for the hook (`lib/auth/__tests__/useAutoRefreshOnRegain.test.ts`) and deduped the regain-focus refresh — superseding this amendment's earlier "wiring-only, no new tests" note. **Round-3 review (2026-08-19, @ssrinivas90):** sign-out no longer fails silently — the screen calls `supabase.auth.signOut()` directly and inspects the resolved `{ error }`, because auth-js keeps the local session on a 5xx/offline failure and `SessionProvider.signOut` discards it; new `lib/auth/performSignOut.ts` + 6 tests, busy/failed states on the control, `SessionProvider`'s public surface untouched (spec decision preserved; `RoleSwitcher`'s identical shape stays with #64). Plus the `inFlight` sync-throw guard, an SSR guard on the exported setup function, `ROLE_POLL_MS` named, and the plan's false "zero new test files / 379-of-58" records struck and corrected. Re-measured post-merge at this head: **vitest 72 files / 479**, tsc clean, `expo lint` clean, secret/tracker/unistyles scans clean; no migrations in the whole diff, so pgTAP is untouched. **Round-4 review (2026-08-20, @ssrinivas90)** blocked on coverage rather than behaviour — the blocker's fix, the SSR guard and the defensive `catch` each survived deletion with a green suite. All now pinned, every mutation re-run to confirm it goes red: new `runSignOut.ts` + 6 tests, a native-`AppState` suite closing the untested half of AC#2 via a local `vi.mock` (5), the SSR/sync-throw cases (+3), and `scripts/_signout-delegation-checks.js` + 7 tests. That last one exists because the reviewer's suggested `runSignOut` extraction, measured, still left the pre-fix revert green — nothing obliges a screen to call a free function, and this repo cannot render one; the source-scan predicate (idiom of `_unistyles-config-checks.js`) is what actually makes the revert fail. Four doc corrections landed with it, incl. replacing the plan's G2 code block, which was still the rejected implementation. **vitest 75 files / 500**, tsc + lint + scans clean, still zero migrations. Ready for `/deploy-staging`. |
| [csv-enrollment-import](csv-enrollment-import.md) | POC-core (thinnest-slice prerequisite) | System | Admin; all personas indirectly (provisioned via import) | enrollment import (CSV baseline) | Test ✓ — 35/35 unit tests, 81 pgTAP, 97/97 RLS adversarial; gate marker written; ready for `/deploy-staging` |
| [notifications-infra](notifications-infra.md) | POC-core (thinnest-slice prerequisite) | System | Student, Parent, Teacher (primary) + Coordinator/BV Coordinator/Admin (participant-scoped, ADR-0016 defaults) | Web Push (VAPID) delivery, keyed off `conversation_participants.notify_level`; SES as Auth-SMTP only (no notification email) | `/design` ✓ — `push-send` contract + client subscribe flow + SES runbook specced; one new security check (caller-must-be-sender) flagged for sign-off; ready for `/plan` |
| realtime-chat-delivery | POC-core (ADR-0007) | System | Student, Teacher, Parent | Supabase Realtime broadcast wiring on durable chat tables | Not started |
| [sankalp-component-kit](sankalp-component-kit.md) | POC-core (foundational — client) | System | all 6 personas' UI (unblocks parallel feed/chat/dashboard/admin/notifications/auth builds) | client presentational layer — full `core/*` + `brand/*` + `bv-connect/components/**` port, no RLS/data access | Built — all 28 components (8 `core/*` + `brand/EventDateBlock` + `core/ListRow`/`StateView` + 16 domain components across comments/privacy/chat/admin/navigation/dashboard/feed/notifications/auth) landed via `superpowers:subagent-driven-development`, each with an implementer→reviewer→fix loop; `app/(dev)/kit-gallery.tsx` harness + route registered; 332/332 vitest, typecheck clean, lint clean, `expo export --platform web` clean, Playwright smoke pass at 360/768/1024/1440 (found + fixed a real `FeedCard` nested-`Pressable`/`<button>` HTML-validity bug); final whole-branch review (opus) — ready to merge, one Important finding (plan's Shared-seam zero-coupling claim didn't hold as built) reconciled in the plan doc; PR pending — blocked by `pr-guard` until `/test` records a pass; ready for `/test` ([plan](sankalp-component-kit.plan.md)) |
| [user-role-approval](user-role-approval.md) | POC-core (doc 2: Coordinator/BV Coordinator/Admin approval flows) | System | Admin, Coordinator, BV Coordinator | privileged role/scope-assignment function (writes `user_roles`) — auto-sweep for CSV-provisioned parents/students + tiered manual grant/revoke for teacher/coordinator/bv_coordinator/admin | Built — review-clean, full suite green (154/154 unit + 141/141 pgTAP); three real access-control bugs (revoke-by-id and coordinator-containment trusting request-body role/scope over the resolved row; `checkAdminRole` keying off any held role instead of the caller's active role) found + fixed + adversarially re-verified during review; live local-stack smoke pass done; ready for `/test` ([plan](user-role-approval.plan.md)) |
| retention-deletion-job | Defer — pending real org/legal sign-off on policy value | System | n/a (infra) | scheduled deletion job against the provisional retention fields | Not started |
| [cicd-pipeline](cicd-pipeline.md) | POC-core (foundational — doc 3 §6.3 migration-guard gate) | System | all (delivery pipeline) | CI/CD + migration-guard/promotion gates | Built — pending `/test` |
| [cloud-environment-provisioning](cloud-environment-provisioning.md) | POC-core (doc 3 §7.1) | System | all | cloud Supabase project A/B + staging/prod env wiring | **Staging live** — console-verified 2026-09-07: Netlify auto-deploys from `main` (US-East functions, env vars set); Supabase healthy, **34/34 migrations** (`local == remote`). Handoff spec on `main` (§4.4 reconciled to ADR-2026-07-30). **Only prod project B remains** — deferred, gated on #66 (ping @mehtamaulik-creator first). |
| error-monitoring-infra | POC-core (doc 2: Admin "error monitoring") | System | Admin | Sentry setup (US region) + PII scrubbing before capture | Not started |
| client-auth-session-and-nav | POC-core (foundational — **client gate**) | System | all 6 personas | client shell: magic-link sign-in + session provider + role-derived nav + active-role switcher (ADR-0005/0014/0023). Server side is `auth-hook-and-identity`; this is the missing client half. | Not started |
| sankalp-component-kit | POC-core (foundational — client) | System | all persona UI | shared component library on Unistyles tokens; the four access-states with no permission-denied screen (ADR-0011/0014) | Not started |
| pilot-seed-data | POC-core (test data) | System | all (dev/test) | realistic seed — Frisco center · F3 session · Shishu Vihaar/KG–Gr12 (13 grades), replacing the 5-band placeholder seed | Not started |
| [test-fixture-isolation](test-fixture-isolation.md) | POC-core (must land before Supabase project B / prod exists — #9 §4.5) | System | every UoW with pgTAP coverage (26 files) + cloud-environment-provisioning (#9) + cicd-pipeline | infra / test-fixture surface — no table, policy, or claim changes | `/refine` ✓ → `/architect` ✓ ([ADR-2026-09-07](../../adr/2026-09-07-test-fixtures-never-in-migrations.md)) → `/design` ✓ (all 2026-09-07, [issue #66](https://github.com/cmdfw-bv/CMDFW-BalaVihar-App/issues/66)) — human sign-off ✓ (2026-09-07) → `/plan` ✓. `tests.create_supabase_user()` is a `SECURITY DEFINER` auth-user factory shipped in a **migration**, so it is live in cloud staging and would apply unchanged to prod B. Not exploitable today (execute revoked from public/anon/authenticated; `tests` absent from `api.schemas`) — filed as a standing escalation primitive that must not sit in a DB holding minors' PII. Decision: drop forward (applied history never edited), fixtures reinstall from the seed path, which `db push` does not load. Two `/architect` corrections to the issue: its ADR-0038 citation was stale, and its "pgTAP test proving a cloud DB has no `tests` schema" is unbuildable — replaced by a CI migration scan + an in-suite lockdown assertion + a **blocking** operator cloud check. `/plan` ✓ → `/migration` ✓ → `/build` ✓ → `/test` ✓ (2026-09-07) — **28 pgTAP files / 352 tests**, 91 vitest files / 701, typecheck + lint + all 5 CI guards clean; verified on an isolated 553xx stack, shared stack untouched. Both new pgTAP files mutation-proved red. AC#1 proved locally too (`db reset --no-seed` *is* a cloud-shaped DB). Next: `/deploy-staging` — staging still carries the fixture surface |
